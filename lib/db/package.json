{
  "name": "@workspace/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "setup-pgvector": "tsx scripts/setup-pgvector.ts",
    "generate": "drizzle-kit generate --config ./drizzle.config.ts",
    "migrate": "pnpm run setup-pgvector && drizzle-kit migrate --config ./drizzle.config.ts",
    "push": "pnpm run setup-pgvector && drizzle-kit push --config ./drizzle.config.ts",
    "push-force": "pnpm run setup-pgvector && drizzle-kit push --force --config ./drizzle.config.ts"
  },
  "dependencies": {
    "drizzle-orm": "catalog:",
    "drizzle-zod": "^0.8.3",
    "pg": "^8.20.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "^8.18.0",
    "drizzle-kit": "^0.31.9",
    "tsx": "^4.20.6"
  }
}
